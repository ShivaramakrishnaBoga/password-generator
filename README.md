🔐 Password Generator (Python)
📌 Project Overview

This project is a command-line based password generator built using Python.
It allows users to create customized and secure passwords by specifying the number of letters, numbers, and symbols they want in the password. The generated password is randomized to improve security.

⚙️ Features

User-defined password length

Custom selection of:

Uppercase & lowercase letters

Numbers

Special symbols

Randomized character order using shuffling

Simple and beginner-friendly implementation

Uses Python’s built-in random module

🧠 How It Works

The program asks the user to enter:

Number of letters

Number of symbols

Number of numbers

Random characters are selected from predefined lists.

All selected characters are stored in a list.

The list is shuffled to ensure randomness.

The final password is generated by joining the characters into a string.

🛠️ Technologies Used

Python 3

random module
